{
  "name": "trackmyjobs",
  "version": "1.0.0",
  "description": "Job Application Tracker",
  "scripts": {
    "start": "cd server && node server.js",
    "build": "cd server && npm install",
    "dev": "cd server && npm run dev",
    "setup-db": "cd server && node setup-render-db.js"
  },
  "engines": {
    "node": "18.x"
  }
}